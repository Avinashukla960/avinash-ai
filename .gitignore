# ---------------------------------------------------------------------
# .gitignore — files we don't want to ever commit to version control.
# ---------------------------------------------------------------------

# Node.js dependencies (huge, regenerated by `npm install`)
node_modules/

# Local environment file containing secrets (API key, session secret)
.env

# SQLite database files
data.db
data.db-journal
data.db-shm
data.db-wal
uploads/
